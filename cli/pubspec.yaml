name: cli
publish_to: none
description: A sample command-line application.
version: 1.0.0
resolution: workspace
# repository: https://github.com/my_org/my_repo

environment:
  sdk: ^3.11.4

# Add regular dependencies here.
dependencies:
  http: ^1.6.0
  command_runner:
    path: ../command_runner # Points to the local command_runner package.

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
